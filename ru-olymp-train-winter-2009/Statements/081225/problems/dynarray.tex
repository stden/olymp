% јвтор: —ергей  опелиович
% “екст: »ль€ –азенштейн

\begin{problem}{ƒинамический массив}
{dynarray.in}{dynarray.out}
{15 секунд}{256 мегабайт}

“ребуетс€ реализовать структуру данных, котора€ представл€ет из себ€
динамический массив, поддерживающий следующие операции:

\begin{enumerate}
\item ѕрисвоить числу, которое стоит на позиции $u$, значение $p$.
\item ¬ставить число $p$ после позиции $u$ (но перед позицией $u + 1$,
 если она существует).
\item —ообщить, сколько чисел, не превосход€щих $p$, стоит на позици€х
 с $u$ по $v$ включительно.
\end{enumerate}

Ёлементы массива нумеруютс€ с единицы.

\InputFile

¬ первой строке наход€тс€ два натуральных числа $n$ и $m$~--- начальное
количество элементов в массиве и количество операций
($1 \le n \le 200\,000$, $1 \le m \le 200\,000$).
¬о второй строке находитс€ $n$ целых чисел $a_1$, $a_2$, $\ldots$, $a_n$
~--- элементы исходного массива ($0 \le a_i \le 10^6$).
—ледующие $m$ строк содержат описани€ операций в формате ``\t{1~u~p}''
($u \ge 1$, $u$ не превосходит текущего размера массива,
$0 \le p \le 10^6$), ``\t{2~u~p}'' ($u \ge 0$, $u$ не превосходит
текущего размера массива, $0 \le p \le 10^6$) или ``\t{3~u~v~p}''
($1 \le u \le v$, $v$ не превосходит текущего размера массива,
$0 \le p \le 10^6$).

\OutputFile

¬ выходном файле должны находитьс€ ответы на запросы ~--- по одному в строке.

\Example

\begin{example}%
\exmp{
5 5
1 3 5 4 2
3 2 4 4
1 3 3
3 2 4 4
2 2 10
3 2 4 4
}{
2
3
2
}%
\end{example}

\end{problem}
