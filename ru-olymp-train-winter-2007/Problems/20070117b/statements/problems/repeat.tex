\begin{problem}{ѕовтор}{repeat.in}{repeat.out}{2 секунды}

¬ы изучаете метод упаковки последовательности маленьких букв латинского алфавита
с помощью замены повтор€ющихс€ кусочков. ј именно, запакованный текст может
кроме исходных букв содержать специальные символы `\texttt{R}' и `\texttt{M}', где
`\texttt{R}' обозначает повторение последовательности, начинающейс€ с последнего символа
`\texttt{M}' (или с начала текста, если символов `\texttt{M}' еще не встречалось).

Ќапример, ``\texttt{abcabcdabcabcdxyxyz}'' можно запаковать как
``\texttt{abcRdRMxyRz}''. ѕри распаковке записываетс€ ``\texttt{abc}'', приписываетс€
к себе, затем записываетс€ символ ``\texttt{d}'', получивша€ последовательность оп€ть
удваиваетс€, после этого записываетс€ ``\texttt{xy}'', повтор€етс€ второй раз, и дописываетс€
``\texttt{z}''.

“ребуетс€ определить минимальное количество символов (с учетом специальных символов
``\texttt{R}'' и ``\texttt{M}''), которое может быть в запакованной последовательности,
распаковывающейс€ в данную.

\InputFile

¬о входном файле задана непуста€ строка из не более чем 50 латинских маленьких букв.

\OutputFile

¬ыведите минимально возможную длину запакованной последовательности.

\Example

\begin{example}
\exmp{
aaaaaaaa
}{
4
}%
\exmp{
aaaaaaa
}{
5
}%
\exmp{
bcdcdcdcdxcdcdcdcd
}{
12
}%
\exmp{
xyz
}{
3
}%
\end{example}

\Note 

¬ третьем примере одним из оптимальных способов €вл€етс€ ``\texttt{bMcdRRxMcdRR}''.

\end{problem}