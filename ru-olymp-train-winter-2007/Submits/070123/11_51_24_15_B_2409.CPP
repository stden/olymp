#include <stdio.h>
#include <string.h>

#define min(a,b) ((a)<(b)?(a):(b))
#define max(a,b) ((a)>(b)?(a):(b))

int main ()
{
  freopen ("omax.in", "rt", stdin);
  freopen ("omax.out", "wt", stdout);

  printf ("NO");

  return 0;
}
