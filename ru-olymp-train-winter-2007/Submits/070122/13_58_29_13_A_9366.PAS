{$Q-;R-;S-;I-;O+;H+}
{$mode delphi}
{$inline on}
uses Math,SysUtils;
begin
	assign(input,'pairs.in'); reset(input);
	close(input);
	assign(output,'pairs.out'); rewrite(output);
	writeln(5);
	close(output);
end.